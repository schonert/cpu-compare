<section class="card overflow-hidden">
  <div class="flex flex-wrap items-center justify-between gap-3 border-b border-line px-5 py-4">
    <h2 class="eyebrow text-accent">Ranked by workload</h2>
    <div class="flex flex-wrap items-center gap-1">
      <% workloads.each do |candidate| %>
        <%= link_to candidate.name, workload_path(candidate.key),
                    class: class_names(tab_classes(candidate)),
                    aria: { current: current_workload?(candidate) ? "true" : nil } %>
      <% end %>
    </div>
  </div>

  <% if scores.empty? %>
    <p class="px-5 py-8 text-center text-sm text-ink-muted">
      Nothing matches. Try a different search, or another workload.
    </p>
  <% else %>
    <div class="overflow-x-auto">
      <table class="w-full border-collapse text-[13px]">
        <thead>
          <tr class="border-b border-line-soft">
            <th class="eyebrow px-5 py-2.5 text-end text-ink-faint">#</th>
            <th class="eyebrow px-5 py-2.5 text-start text-ink-faint">Processor</th>
            <th class="eyebrow px-5 py-2.5 text-start text-ink-faint">
              <%= workload&.name %> &middot; <%= version&.label %>
            </th>
            <th class="eyebrow px-5 py-2.5 text-end text-ink-faint">Compare</th>
          </tr>
        </thead>
        <tbody>
          <% scores.each_with_index do |score, index| %>
            <tr class="border-b border-line-soft last:border-b-0 hover:bg-sunken">
              <td class="figure px-5 py-3 text-end text-ink-faint"><%= rank_for(index) %></td>
              <td class="px-5 py-3">
                <span class="flex items-center gap-2">
                  <%= render VendorDotComponent.new(vendor: score.cpu.vendor) %>
                  <span class="font-medium text-ink"><%= score.cpu.name %></span>
                </span>
              </td>
              <td class="px-5 py-3">
                <%# Value, units, sample count, version, date and source — all
                    of it, on every row. %>
                <%= render FigureComponent.new(score: score) %>
              </td>
              <td class="px-5 py-3 text-end">
                <% if selected?(score.cpu) %>
                  <span class="eyebrow text-ink-faint">Selected</span>
                <% elsif full? %>
                  <span class="eyebrow text-ink-faint">4 max</span>
                <% else %>
                  <%= link_to "Add", compare_path_with(score.cpu.slug, selected: selected,
                                                       version: version&.to_param),
                              class: "rounded-full bg-sunken px-3 py-1 text-[12px] font-medium text-ink hover:bg-ink hover:text-panel" %>
                <% end %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  <% end %>
</section>
