<div class="flex flex-wrap items-center gap-1">
  <% versions.each do |version| %>
    <%= link_to version.label, path_for(version),
                class: class_names(classes(version)),
                aria: { current: current?(version) ? "true" : nil } %>
  <% end %>
</div>
